<%= form_with(model: status, local: true) do |form| %>
  <% if status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :answered %>
    <%= form.check_box :answered %>
  </div>

  <div class="field">
    <%= form.label :hint1 %>
    <%= form.check_box :hint1 %>
  </div>

  <div class="field">
    <%= form.label :hint2 %>
    <%= form.check_box :hint2 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
